> beforeEach: runs before each it block
> before: runs before the next it block that appears in the file
> afterEach: runs after each it block
> after: runs after the it block that appears before it in the file.